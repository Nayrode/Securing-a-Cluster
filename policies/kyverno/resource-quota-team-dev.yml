apiVersion: v1
kind: ResourceQuota
metadata:
  name: team-dev-pod-quota
  namespace: team-dev
spec:
  hard:
    pods: "5"
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
